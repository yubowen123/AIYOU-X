# AIYOU Logo source

The user selected concept 1 from the GPT Image 2 exploration board on 2026-08-31.

- `selection-1-reference.png` is the direct selection reference supplied by the user. It is not used
  as a package asset because it is a small screenshot with a selection number.
- `../icon.svg` is the cleaned, scalable production master redrawn from that selection.
- `../icon.png`, `../icon.icns`, and `../icon.iconset/` are generated package assets.
- The in-app `BrandMark` imports the same SVG master so the product and installer stay consistent.

The mark uses a warm blush squircle, a coral AIYOU ribbon and a butter-yellow crossing stroke. It does
not reuse WorkRally's white W mark or any third-party trademark asset.
